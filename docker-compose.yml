services:
  rural-innovation-hub:
    build: .
    container_name: rural-innovation-hub
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
      JWT_SECRET: change-this-secret-in-production
    volumes:
      - ./data:/app/data
    restart: unless-stopped
